<body>

<% if  @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:
<ul>
  <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
</ul>
<% end %>



<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

<label>Title</label>
<%= f.text_field :title %>

<label>Body</label>
<%= f.text_area :body %>

<span class=submit_form><%= f.submit "Update Book" %></span>

<% end %>



<%= link_to "Show", book_path(@book.id) %>
|
<%= link_to "Back", "/books" %>

</body>